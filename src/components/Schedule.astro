---
import { getCollection } from "astro:content";
import { formatDates } from "@/utils.js";
import Schedule from "@/components/Schedule.jsx";

interface Props {
    group?: string;
    count?: number;
    ascending?: boolean;
    showOld?: boolean;
}

const { group, count, ascending = true, showOld = false } = Astro.props;

const allEvents = await getCollection("schedule");
console.log(count);
---

<Fragment>
    <Schedule data={allEvents} count={count} group={group} ascending={ascending} showOld={showOld} client:load />
    <noscript>
        Sorry, schedule requires JavaScript at this time.
    </noscript>
</Fragment>
