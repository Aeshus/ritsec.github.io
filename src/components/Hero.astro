---

---

<section>
    <div>
        <p class="message">Welcome to the spring semester!</p>
        <h1>Security through <strong>Community</strong></h1>
        <p class="copy">
            Student-run. Community focused. Hosting competitions and conducting
            research. We break things to learn how to fix them.
        </p>
        <div class="buttons">
            <a class="link-button primary" href="/join">GET INVOLVED</a>
            <a class="link-button secondary" href="/sponsors">SPONSOR US</a>
        </div>
    </div>

    <div class="image">
        <img src="/image7.jpg" alt="Letchworth State Park landscape" />
        <p class="hero-caption">Letchworth State Park, 2024</p>
    </div>
</section>

<style>
    section {
        padding: 2rem var(--padding);
        display: grid;
        grid-template-columns: 1.2fr 1fr;
        align-items: center;
        gap: 2rem;
        border-bottom: 2px solid var(--border);
        background-color: var(--bg);
    }

    .message {
        background-color: var(--bg-panel);
        display: inline-block;
        color: var(--fg);
        padding: 0.25rem 0.5rem;
        margin-bottom: 1rem;
        font-weight: 500;
    }

    h1 {
        font-size: 4rem;
        line-height: 1.1;
        margin-bottom: 1rem;
    }

    h1 strong {
        color: var(--accent);
    }

    .copy {
        font-size: 1.125rem;
        max-width: 60ch;
        margin-bottom: 2rem;
    }

    .buttons {
        display: flex;
        flex-wrap: wrap;
        gap: 1rem;
    }

    .image {
        padding: 0.5rem;
        outline: 2px solid var(--border);
        outline-offset: -1px;
    }

    img {
        display: block;
        width: 100%;
        height: auto;
    }

    .caption {
        font-family: var(--font-mono);
        font-size: 0.875rem;
        margin-top: 0.5rem;
        color: var(--fg);
    }

    @media (max-width: 1000px) {
        section {
            grid-template-columns: 1fr;
            text-align: center;
        }

        .copy {
            margin: auto;
            margin-bottom: 2rem;
        }

        .buttons {
            justify-content: center;
        }

        h1 {
            font-size: 2.5rem;
        }

        .image {
            margin-inline: auto;
        }
    }
</style>
