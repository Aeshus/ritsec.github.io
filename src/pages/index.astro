---

---

<html lang="en">
	<head>
		<meta charset="utf-8" />
		<link rel="icon" type="image/svg+xml" href="/favicon.svg" />
		<meta name="viewport" content="width=device-width" />
		<meta name="generator" content={Astro.generator} />
		<title>RITSEC</title>
        <link rel="preconnect" href="https://fonts.googleapis.com">
        <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
        <link href="https://fonts.googleapis.com/css2?family=IBM+Plex+Mono:ital,wght@0,100;0,200;0,300;0,400;0,500;0,600;0,700;1,100;1,200;1,300;1,400;1,500;1,600;1,700&family=IBM+Plex+Sans:ital,wght@0,100..700;1,100..700&display=swap" rel="stylesheet">
	</head>
	<body>
        <header>
            <div>
                <a href="/">
                    <img src="ritsec_logo_final-05.png" />
                </a>
                <input type="checkbox" id="nav-toggle-checkbox">

                <label for="nav-toggle-checkbox" class="nav-toggle-label">
                    <span>☰</span>
                </label>
                <nav>
                    <a href="/">Home</a>
                    <a href="/about">About</a>
                    <a href="/events">Events</a>
                    <a href="/groups">Groups</a>
                    <a href="/schedule">Schedule</a>
                    <a href="/research">Research</a>
                    <a href="/sponsors">Sponsors</a>
                    <a href="/join" class="call-to-action">Join Us</a>
                </nav>
            </div>
        </header>
        <main>
            <div class="hero">
                <div>
                    <div>
                        <p class="hero-message">Welcome to the Spring semester!</p>
                        <h1>Security through <strong>Community</strong></h1>
                        <p class="hero-copy">Student-run. Community focused. Hosting competitions and conducting research. We break things to learn how to fix them.</p>
                        <div class="hero-buttons">
                            <a class="link-button filled" href="/join">BECOME A MEMBER</a>
                            <a class="link-button unfilled" href="/sponsors" >SPONSOR US</a>
                        </div>
                    </div>

                    <div class="hero-image">
                        <img src="/image7.jpg">
                        <p class="hero-caption">Letchworth State Park, 2024</p>
                        <!-- left and right controls to go between images -->
                    </div>
                </div>
            </div>

            <!-- Interest Groups -->

            <!-- Events -->

            <!-- Schedule -->

            <!-- Research -->

            <!-- Sponsors -->
        </main>
        <footer>
        </footer>
	</body>
</html>

<style is:global>
/*
  Josh's Custom CSS Reset
  https://www.joshwcomeau.com/css/custom-css-reset/
*/

*, *::before, *::after {
  box-sizing: border-box;
}

*:not(dialog) {
  margin: 0;
}

@media (prefers-reduced-motion: no-preference) {
  html {
    interpolate-size: allow-keywords;
  }
}

body {
  line-height: 1.5;
  -webkit-font-smoothing: antialiased;
}

img, picture, video, canvas, svg {
  display: block;
  max-width: 100%;
}

input, button, textarea, select {
  font: inherit;
}

p, h1, h2, h3, h4, h5, h6 {
  overflow-wrap: break-word;
}

p {
  text-wrap: pretty;
}
h1, h2, h3, h4, h5, h6 {
  text-wrap: balance;
}

#root, #__next {
  isolation: isolate;
}

/* ----------------------------- */

/* ----------------------------- */

:root {
    --bg: #fefefe;
    --bg-panel: #eeeeee;

    --fg: #000;
    --accent: #E69132;

    --page-width: 120ch;

    --font-mono: "IBM Plex Mono", monospace;
    --font-sans: "IBM Plex Sans", sans-serif;

    font-family: var(--font-sans);
}

pre {
    font-family: var(--font-mono);
}

@media (prefers-color-scheme: dark) {
    :root {
        --bg: #0c0c0c;
        --bg-panel: #545454;

        --fg: #dfdfdf;
    }
}

body {
    color: var(--fg);
    background-color: var(--bg);
    min-height: 100%;
    display: flex;
    flex-direction: column;
}

html {
    min-height: 100%;
    height: 100%;
}

main {
    flex-grow: 1;
}

header {
    position: sticky;
    top: 0;
    padding: 0.25rem 0;
    border-bottom: 1px solid var(--bg-panel);
    background-color: var(--bg);
}

header div {
    display: flex;
    flex-direction: row;
    align-items: center;
    justify-content: space-between;
    max-width: calc(min(100% - 2rem, var(--page-width)));
    width: 100%;
    margin: auto;
}

header div > * {
    font-size: 1.125rem;
}

header img {
    height: 3.25rem;
    width: 100%;
}

header div > a {
    display: inline-block;
}

header div > a:hover {
    background-color: var(--bg-panel);
}

header nav {
    display: flex;
    flex-direction: row;
    gap: 0.75rem;
}

header nav a {
    text-decoration: none;
    padding: 0.25rem 0.5rem;
    color: inherit;
    display: inline-block;
    font-family: var(--font-mono);
}

header nav a:hover {
    background-color: var(--accent);
    color: var(--bg);
}

.call-to-action {
    background-color: var(--fg);
    color: var(--bg);
}
.call-to-action:hover {
    background-color: var(--accent);
    color: var(--bg);
}

#nav-toggle-checkbox {
  display: none;
}

.nav-toggle-label {
    display: none; 
}

@media (max-width: 1000px) {
    header div {
        flex-wrap: wrap;
    }

    header nav {
        display: none;
        width: 100%; 
        flex-direction: column; 
        margin-top: 0.5rem;
    }

    #nav-toggle-checkbox:checked ~ nav {
        display: flex;
    }

    .nav-toggle-label {
        display: block;
        cursor: pointer; 
        user-select: none;
    }

    .hero h1 {
        font-size: 3rem !important;
    }
}

/* ------------------------- */

.hero {
    padding: 1rem 0;
    border-bottom: 1px solid var(--bg-panel);
}

.hero > div {
    max-width: calc(min(100% - 2rem, var(--page-width)));
    width: 100%;
    margin: auto;
    display: grid;
    grid-template-columns: 1fr 1fr;
    align-items: center;
    gap: 1rem;
}

@media (max-width: 1000px) {
    .hero > div {
        display: flex;
        flex-direction: column;
        align-items: unset;
    }
}

.hero-message {
    background-color: var(--bg-panel);
    display: inline-block;
    color: var(--fg);
    padding: 0.25rem 0.5rem;
    margin: 0;
}

.hero h1 {
    font-size: 4rem;
    line-height: 1.125;
}

h1 strong {
    color: var(--accent);
}

.hero-buttons {
    display: flex;
    flex-direction: row;
    gap: 1rem;
    margin-top: 2rem;
}

.link-button {
    padding: 0.25rem 0.5rem;
    display: inline-block;
    text-decoration: none;
}

.link-button.filled {
    color: var(--bg);
    background-color: var(--fg);
}
.link-button.filled:hover {
    background-color: var(--accent);
}

.link-button.unfilled {
    color: inherit;
    outline: 1px solid var(--fg);
    outline-offset: -1px;
}
.link-button.unfilled:hover {
    background: var(--bg-panel);
}

.hero-image {
    padding: 0.5rem;
    outline: 1px solid var(--fg);
    outline-offset: -1px;
}

.hero-image-caption {
    font-family: var(--font-mono);
}

</style>
