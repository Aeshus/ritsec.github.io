---
---

import { Picture } from "astro:assets";

import Hero from "@/components/Hero.astro";
import GroupCarousel from "@/components/GroupCarousel.astro";
import Schedule from "@/components/Schedule.astro";
import EventsCarousel from "@/components/EventsCarousel.astro";
import ResearchList from "@/components/ResearchList.astro";
import SponsorsList from "@/components/SponsorsList.astro";
import SponsorsCarousel from "@/components/SponsorsCarousel.astro";
import BaseLayout from "@/layouts/BaseLayout.astro";

import heroImg from "@/assets/image7.jpg";

<BaseLayout title="Home" image={heroImg}>
    <Hero 
            badge="Welcome to the spring semester!"
            image={heroImg}
            imageAlt="Letchworth State Park landscape"
        >
            <Fragment slot="title">
                Security Through **Community**
            </Fragment>

            Student-run. Community focused. Hosting competitions and conducting research. We break things to learn how to fix them.

            <Fragment slot="bottom">
                <a class="link-button primary" href={import.meta.env.BASE_URL === "/" ? "/join" : import.meta.env.BASE_URL.replace(/\/$/, "") + "/join"}>GET INVOLVED</a>
                <a class="link-button secondary" href={import.meta.env.BASE_URL === "/" ? "/sponsors" : import.meta.env.BASE_URL.replace(/\/$/, "") + "/sponsors"}>SPONSOR US</a>
            </Fragment>

            <Fragment slot="image">
                <Picture src={heroImg} width={1000} formats={['avif', 'webp']} alt="Fall 2024 RITSEC Letchworth State Park group photo" />
            </Fragment>
    </Hero>

    <section class="copy">
        ## Interest Groups

        Beyond our general meetings, we run specialized sub-groups that dive deep into specific domains of cybersecurity.

        <GroupCarousel />

    </section>

    <section class="copy">
        ## Schedule

        Stay up to date with our weekly meetings, workshops, and interest group sessions.

        <Schedule count={3} daysAhead={14} />

        <a class="link-button primary" href={import.meta.env.BASE_URL === "/" ? "/education" : import.meta.env.BASE_URL.replace(/\/$/, "") + "/education"}>VIEW EDUCATION</a>
    </section>

    <section class="copy">
        ## Events

        Home to nationally recognized Attack/Defense competitions, Capture the Flags, and our exclusive Career Fair Eve networking event.

        <EventsCarousel />
    </section>

    <section class="copy">
        ## Research

        Pushing the boundaries of security through student-led innovation and blog posts.

        <ResearchList count={3} />

        <a class="link-button primary" href={import.meta.env.BASE_URL === "/" ? "/research" : import.meta.env.BASE_URL.replace(/\/$/, "") + "/research"}>VIEW ALL RESEARCH</a>
    </section>

    <section class="copy">
        ## Sponsors

        It is thanks to our sponsors that we are able to do what we do best.

        <SponsorsCarousel />
    </section>
</BaseLayout>
